<h1>Blog Home</h1>
<% @posts.each do |post|%>
  <div class='post'>
    <h2><%= link_to post.title, post %> - <%= post.created_at.strftime("%d %b %Y") %></h2>
    <p><%= post.body %></p>
  </div>
<% end %>